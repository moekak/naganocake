<div class="container">
  <div class="row mt-5">
    <div class="genre_container">
      <h5 class='genre_name'>ジャンル検索</h5>
      <% @genres.each do |genre| %>
        <div class="genre_info py-2">
          <%= genre.name %>
        </div>
      <% end %>
    </div>
    <div class="col offset-1">
      <h4>ようこそながのCAKEへ！<br>このサイトは、ケーキの販売ECサイトになります。<br>会員でない方も商品の閲覧はできますが、<br>購入には会員登録が必要になります。</h4>
    </div>
  </div>
  <div class='row mt-3 offset-3'>
    <div class="container">
      <div class="row">
        <div class="col">
          <h3><span>新着商品</span></h3>
        </div>
      </div>
      <% @items.each do |item| %>
      <div class="row">
        <div class="col">
          <% if item.image.attached? %>
            <%= image_tag item.image, size: "200x200" %>
          <% else %>
            <%= image_tag 'no_image', size: "200x200" %>
          <% end %>
          <p><%= item.name %></p>
          <p><%= item.price %>￥</p>
        </div>
      <% end %>
      </div>
    </div>
  </div>
</div>